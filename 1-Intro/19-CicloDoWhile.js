// Ciclo do while
// Imprimir los valores del 0 al 5
//Do while permite que se ejecute el ciclo almenos una vez.
let contador = 0, repeticiones = 5;
do{
    console.log(contador++);
    //contador++;
}while(contador <= repeticiones);
